name: R2 Lab Autoselect App First
on:
  workflow_dispatch:
permissions: {}
jobs:
  emit:
    name: r2-lab-autoselect-app-first-v12
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Emit fixed lab dispatch check
        run: 'true'
